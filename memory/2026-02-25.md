# Memory — 2026-02-25

## Polymarket Spread-Arb Bot — Session Notes

### Bot Location & Start Command
- Bot: `/root/polymarket-bot/` (NOT workspace)
- Start: `rm -f /tmp/polymarket-bot.lock && nohup /root/polymarket-bot/run.sh > /tmp/polymarket-bot.log 2>&1 &`
- Kill: `kill -9 $(ps aux | grep run.sh | grep -v grep | awk '{print $2}'); kill $(lsof -ti:8080); rm -f /tmp/polymarket-bot.lock`
- Dashboard: `http://178.156.248.17:8080`
- Proxy: `http://lsuysski:zsuzuo6md8b7@142.111.67.146:5611`

### Funder / Signer
- Funder: `0xf30aaAf3B1ADaa5FCfA941891C8c4e05174B6884`
- Signer: `0xc6CD220D744e3CCc7195e544cFf5C27FEc6c59dE`
- Account slug: `0xf30aaAf3B1ADaa5FCfA941891C8c4e05174B6884-1771971337765`

### Polymarket Portfolio PnL API — SOLVED
The correct endpoint data is embedded in the profile page's `__NEXT_DATA__`:

```
GET https://polymarket.com/profile/0xf30aaaf3b1adaa5fcfa941891c8c4e05174b6884
Parse: <script id="__NEXT_DATA__"> → props.pageProps.dehydratedState.queries
```

Key query keys found in __NEXT_DATA__:
- `['portfolio-pnl', slug, wallet, '1D']` → time-series `[{t: timestamp, p: pnl_value}, ...]` (hourly)
- `['portfolio-pnl', slug, wallet, 'ALL']` → same format, full history
- `['/api/profile/volume', wallet, wallet]` → `{amount, pnl: 3.154, realized: 0, unrealized: 0}`
- `['positions', 'value', wallet]` → current position value ($13.43)

The `portfolio-pnl` series data IS pre-rendered in the page — no separate API call needed; scrape it from __NEXT_DATA__.

The `pnl` field in `/api/profile/volume` response = **all-time realized PnL** ($3.15 at time of check). Polymarket showed $4.99 in the UI — the UI includes unrealized PnL on top of realized.

**Correct all-time PnL formula:**
`pnl_from_volume_endpoint + sum(open_position cashPnl)`
= $3.15 + (open unrealized) ≈ $4.99 approx

The `/api/profile/volume` Next.js route returns 404 when called directly — it's an internal SSR route, not a public API. Must parse from __NEXT_DATA__ instead.

### What Was Changed Today
1. `Balance` stat → `Net Worth` (free USDC + position values)
2. PnL source changed: now uses `cashPnl + realizedPnl` from Polymarket positions API + `closedPnl` accumulator
3. `closedPnl` accumulator: locked in per-position PnL when positions disappear from API (resolved/sold)
4. `pnl.json` structure changed — old baseline-based file deleted; fresh start with new schema
5. Bot restarted at ~20:40 UTC

### PnL at Restart (~20:40 UTC)
- Free USDC: $9.74
- Position value: $13.72
- Net Worth: $23.46
- All-time PnL (from positions API): -$2.20 (open cashPnl + realizedPnl for open positions)
- Polymarket UI showed: +$4.99 (1D) — discrepancy because Polymarket includes realized PnL from closed positions

### TODO (Next Session)
- [ ] Fix all-time PnL to scrape `__NEXT_DATA__` from profile page for `portfolio-pnl` series + `pnl` from volume query
- [ ] OR: implement correct formula: fetch `/api/profile/volume` pnl field (if accessible) + sum(open cashPnl)
- [ ] The internal Next.js API routes (`/api/profile/volume`, `/api/portfolio-pnl`) return 404 directly — must scrape from page pre-render
- [ ] Consider adding `resetBaseline` endpoint for new USDC deposits
- [ ] Bot balance $9.74 — just below $9.80 threshold; will auto-resume when a tiny fill clears

### Activity Stats (as of 20:41 UTC)
- Total trades: 65 events (started 05:17 UTC)
- Total BUY: $68.30, SELL: $57.15
- Account joined: 2026-02-24T17:15 UTC (yesterday)
